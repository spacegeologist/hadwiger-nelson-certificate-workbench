# 2026-06-18 Kickoff

## Decision

Primary target: Hadwiger-Nelson problem through finite unit-distance graph certificates.

Breakthrough target: find a finite unit-distance graph that is not 5-colorable, which would raise the lower bound for the chromatic number of the plane from 5 to 6.

## Built Today

- Pure-Python graph representation.
- Exhaustive k-colorability verifier with color symmetry pruning.
- Approximate unit-edge embedding checker.
- Moser spindle baseline as built-in example and JSON data.
- CLI entry point for k-colorability checks.

## Verification

Commands run:

```bash
python3 -m unittest discover -s tests -v
python3 scripts/verify_graph.py --example moser --colors 3
python3 scripts/verify_graph.py --example moser --colors 4
```

Results:

- Test suite: 5 tests passed.
- Moser spindle with 3 colors: NOT colorable.
- Moser spindle with 4 colors: colorable.
- 4-coloring found:
  - x: 0
  - a: 1
  - b: 2
  - y: 0
  - c: 1
  - d: 2
  - z: 3

## Next Moves

1. Add DIMACS CNF export for k-colorability so external SAT solvers can cross-check hard cases.
2. Add a graph minimizer that removes vertices while preserving non-k-colorability.
3. Locate and import a known 5-chromatic unit-distance graph dataset from de Grey/Polymath16 sources.
4. Reproduce non-4-colorability for that known graph locally.
5. Only after reproduction, begin candidate search for smaller or more structured examples.
